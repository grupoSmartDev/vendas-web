import { PendingTasksPage } from '@/components/tasks/pending-tasks-page';

/**
 * Rota /tasks
 * Lista todas as tarefas pendentes
 */
export default function TasksPage() {
    return <PendingTasksPage />;
}